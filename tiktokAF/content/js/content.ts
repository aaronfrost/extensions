import './location-utils';
import '../../../libs/live-reload-content';
import './wip';
import './detectMode';

// @ts-ignore
window.LiveReloadOptions = {
    isChromeExtension: true,
    host: 'localhost',
};
